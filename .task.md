## Current Task
Recorder-like ASR benchmark page and Sherpa ASR config cleanup

## Status
needs-validation

## Approach
Isolate the experiment in a dedicated sherpa-voice benchmark screen, reuse the existing ASR/model plumbing, and expose model-specific ASR options through the RN bridge so different models can be benchmarked without hardcoded defaults.

## Progress
- Created branch `feat/recorder-like-transcription-research`
- Mapped the current ASR UI, model catalog, and native/web ASR bridge paths
- Confirmed Android already has low-level Canary and richer offline model support
- Identified config gaps: Whisper task/language hardcoded, SenseVoice web flag mismatch, no benchmark-specific page
- Added a dedicated `ASR Benchmark` feature page with sample-file and live-mic benchmark flows
- Added a benchmark matrix that includes practical live models plus offline reference models, not only likely winners
- Expanded the app model catalog with Kroko, SenseVoice, and Canary benchmark entries
- Updated web and Android ASR handlers so Whisper, SenseVoice, and Canary options can reach the runtime
- Recorded follow-up package gaps in `docs/ASR_BENCHMARK.md` (`iOS bridge/codegen`, vendored Sherpa refresh)

## Last Updated
2026-04-01 22:16 HKT
