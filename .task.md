## Current Task
Web Worker implementation improvements (7 changes from code review)

## Status
done

## Approach
Implemented all 7 design improvements: shared singleton worker, single-source ASR switch, lazy WASM load, deduplicated config, configurable model filenames, clean transferables API, and useWorker config option.

## Progress
- All 7 changes implemented and building clean (no TS errors)
- CDP-validated diarization E2E on sherpa-voice web: 2 speakers detected, init 151ms (cached), process 8.7s
- CDP-validated shared worker: both diarization + denoising init on single Worker instance
- CDP-validated release sequence: both features release cleanly from shared worker
- No UI errors in screenshot

## Last Updated
2026-03-30T01:40Z
