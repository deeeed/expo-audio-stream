## Current Task
Add dismiss-dev-menu support to agentic scripts in both apps

## Status
done

## Approach
Add `dismiss-dev-menu|dismiss` command to device-cmd.sh (Escape key on iOS, Back key on Android). Add prevention defaults to wake-devices.sh (write EXDevMenuIsOnboardingFinished + EXDevMenuDisableAutoLaunch on iOS, SharedPreferences on Android).

## Progress
- Updated device-cmd.sh in both apps: added `dismiss` command with --platform flag
- Updated wake-devices.sh in both apps: added prevention defaults after deeplink wake
- iOS dismiss: writes defaults (always works) + tries osascript Escape (needs Accessibility permission)
- Android dismiss: adb keyevent KEYCODE_BACK
- Smoke tested: iOS dismiss writes defaults correctly, Android BACK key sent
- Verified defaults persisted on simulator: EXDevMenuIsOnboardingFinished=1

## Last Updated
2026-03-11
